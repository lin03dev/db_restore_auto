git config --global user.name "teliosgza-source"
git config --global user.email "telios.gza@gmail.com"

git config --list

git remote set-url origin https://github.com/teliosgza-source/db_restore_auto.git

git remote -v